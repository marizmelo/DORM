/*  
  DORM (Document Object Render Model)
  author: Mariz Melo
  github: marizmelo/DORM
*/
var cheerio = require('cheerio')
  , DORM = function() {}  // name-space
  , d = DORM.prototype;

d.controller = {
  _render : function(obj) {
    if('head' in obj){

    }
  }
}